services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: sync-bridge-backend
    ports:
      - '8085:8000'
    volumes:
      - ./backend:/var/www/html
    environment:
      - APP_ENV=local
      - APP_DEBUG=true
      - DB_CONNECTION=sqlite
      - DB_DATABASE=/var/www/html/database/database.sqlite
      - INTERNAL_SECRET=54e34dbdc6e374cc8bc74da00faab8fd020ee804e050cecd437ed90e25f05d5e
    command: php artisan serve --host=0.0.0.0 --port=8000
  shopify-app:
    build:
      context: ./shopify-app
      dockerfile: Dockerfile
    container_name: sync-bridge-shopify
    ports:
      - '3005:3005'
    env_file:
      - ./shopify-app/.env
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
      - INTERNAL_SECRET=54e34dbdc6e374cc8bc74da00faab8fd020ee804e050cecd437ed90e25f05d5e
